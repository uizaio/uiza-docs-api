## Convert into VOD

> REQUEST

```shell
curl -X POST \
  '<%= config[:endpoint] %>/live/entity/dvr/convert-to-vod?id=3fec45e9-932b-4efe-b97f-dc3053acaa05' \
  -H 'Authorization: <%= config[:token] %>'
```

```ruby
require "uiza"

Uiza.app_id = "<%= config[:app_id] %>"
Uiza.authorization = "<%= config[:token] %>"

begin
  live = Uiza::Live.convert_to_vod "<%= config[:live_convert_to_vod] %>" # Identifier of record (get from list record)
  puts live.id
rescue Uiza::Error::UizaError => e
  puts "description_link: #{e.description_link}"
  puts "code: #{e.code}"
  puts "message: #{e.message}"
rescue StandardError => e
  puts "message: #{e.message}"
end
```

```python
import uiza

from uiza.api_resources.live import Live
from uiza.exceptions import ServerException

uiza.app_id = "<%= config[:app_id] %>"
uiza.authorization = "<%= config[:token] %>"

try:
  res, status_code = Live().convert_into_vod(id="<%= config[:live_delete_record_id] %>") # Identifier of record (get from list record)

  print("res: ", res)
except ServerException as e:
  raise e
except Exception as e:
  raise e
```

```php
<?php
require __DIR__."/../vendor/autoload.php";

Uiza\Base::setAppId("<%= config[:app_id] %>");
Uiza\Base::setAuthorization("<%= config[:token] %>");

try {
  $entity = Uiza\Live::convertToVOD(["id" => "<%= config[:live_delete_record_id] %>"]); // Identifier of record (get from list record)

  print_r($entity);
} catch(\Uiza\Exception\ErrorResponse $e) {
  print($e);
}
?>
```

```java
import java.util.*;
import com.google.gson.*;

import io.uiza.Uiza;
import io.uiza.exception.*;
import io.uiza.model.Live;
import io.uiza.model.Live.*;

public class Main {

  public static void main(String[] args) {
    Uiza.appId = "<%= config[:app_id] %>";
    Uiza.authorization = "<%= config[:token] %>";

    try {
      JsonObject response = Live.convertToVod("<%= config[:live_convert_to_vod] %>");
      System.out.println(response);
    } catch (UizaException e) {
      System.out.println("Status is: " + e.getStatusCode());
      System.out.println("Message is: " + e.getMessage());
      System.out.println("Description link is: " + e.getDescriptionLink());
    } catch (Exception e) {
      System.out.println(e);
    }
  }
}
```

```javascript
const uiza = require('uiza');

uiza.app_id('<%= config[:app_id] %>');
uiza.authorization('<%= config[:token] %>');

const params = {
  'id': '<%= config[:live_convert_to_vod] %>'
};

uiza.live.convert_to_vod(params) // Identifier of record (get from list record)
  .then((res) => {
    // Identifier of record (get from list record)
  }).catch((err) => {
    //Error
  });
```

```go
import (
  "github.com/uizaio/api-wrapper-go"
  "github.com/uizaio/api-wrapper-go/live"
)

func init() {
  Uiza.AppID = "<%= config[:app_id] %>"
  Uiza.Authorization = "<%= config[:token] %>"
}

param := &uiza.LiveIDParams{ID: uiza.String("<%= config[:live_convert_to_vod] %>")} // Identifier of record (get from list record)
response, err := live.ConvertToVOD(param)
if err != nil {
  log.Printf("%v\n", err)
} else {
  log.Printf("%v\n", response)
}
```

```csharp
using System;
using Uiza.Net.Configuration;
using Uiza.Net.Enums;
using Uiza.Net.Parameters;
using Uiza.Net.Services;

UizaConfiguration.SetupUiza(new UizaConfigOptions
{
  Authorization = "<%= config[:token] %>",
  AppId = "<%= config[:app_id] %>"
});

try
{
  var result = UizaServices.Live.ConvertToVOD("<%= config[:live_convert_to_vod] %>");

  Console.WriteLine(string.Format("Convert VOD Success", result.Data.id));
  Console.ReadLine();
}
catch (UizaException ex)
{
  Console.WriteLine(ex.Message);
  Console.ReadLine();
}
```

Convert a recorded file into [entity](#video).

> RESPONSE

```json
{
  "id": "03739912-d781-4d5a-aaf8-7262691a5d0c"
}
```

**HTTP Request**

<span class="post-button"> POST </span>
```<%= config[:endpoint] %>/live/entity/dvr/convert-to-vod```

**ARGUMENTS**


| Parameter   | Description |
|-------------|-------------------------|
| **id** *string* | The ID of recorded file taken from [list record](#list-record) |

**RETURNS**

Returns an ID of the entity on success. Otherwise, this call returns an error.
