## Add a storage

Uiza allows you to add storages (FTP, S3 - both private and AWS) to your application. Storages can be used for [creating entities](#create-entity).

> **Add a FTP Storage**

> REQUEST

```shell
curl -X POST \
  <%= config[:endpoint] %>/media/storage \
  -H 'Authorization: <%= config[:token] %>' \
  -H 'Content-Type: application/json' \
  -d '{
    "appId": "<%= config[:app_id] %>",
    "name": "<%= config[:storage_add_fpt_name] %>",
    "description": "<%= config[:storage_add_fpt_description] %>",
    "storageType": "<%= config[:storage_add_fpt_storage_type] %>",
    "host": "<%= config[:storage_add_fpt_host] %>",
    "username": "<%= config[:storage_add_fpt_username] %>",
    "password": "<%= config[:storage_add_fpt_password] %>",
    "port": <%= config[:storage_add_fpt_port] %>
}'
```

```ruby
require "uiza"

Uiza.app_id = "<%= config[:app_id] %>"
Uiza.authorization = "<%= config[:token] %>"

params = {
  name: "<%= config[:storage_add_fpt_name] %>",
  description: "<%= config[:storage_add_fpt_description] %>",
  storageType: "<%= config[:storage_add_fpt_storage_type] %>",
  host: "<%= config[:storage_add_fpt_host] %>",
  username: "<%= config[:storage_add_fpt_username] %>",
  password: "<%= config[:storage_add_fpt_password] %>",
  port: <%= config[:storage_add_fpt_port] %>
}

begin
  storage = Uiza::Storage.add params
  puts storage.id
  puts storage.name
rescue Uiza::Error::UizaError => e
  puts "description_link: #{e.description_link}"
  puts "code: #{e.code}"
  puts "message: #{e.message}"
rescue StandardError => e
  puts "message: #{e.message}"
end
```

```python
import uiza

from uiza.api_resources.storage import Storage
from uiza.exceptions import ServerException

uiza.app_id = "<%= config[:app_id] %>"
uiza.authorization = "<%= config[:token] %>"

storage_data = {
  "name": "<%= config[:storage_add_fpt_name] %>",
  "description": "<%= config[:storage_add_fpt_description] %>",
  "storageType": "<%= config[:storage_add_fpt_storage_type] %>",
  "host": "<%= config[:storage_add_fpt_host] %>",
  "username": "<%= config[:storage_add_fpt_username] %>",
  "password": "<%= config[:storage_add_fpt_password] %>",
  "port": <%= config[:storage_add_fpt_port] %>
}

try:
  res, status_code = Storage().add(**storage_data)

  print("res: ", res)
except ServerException as e:
  raise e
except Exception as e:
  raise e
```

```php
<?php
require __DIR__."/../vendor/autoload.php";

Uiza\Base::setAppId("<%= config[:app_id] %>");
Uiza\Base::setAuthorization("<%= config[:token] %>");

$params = [
  "name" => "<%= config[:storage_add_fpt_name] %>",
  "description" => "<%= config[:storage_add_fpt_description] %>",
  "storageType" => "<%= config[:storage_add_fpt_storage_type] %>",
  "host" => "<%= config[:storage_add_fpt_host] %>",
  "username" => "<%= config[:storage_add_fpt_username] %>",
  "password" => "<%= config[:storage_add_fpt_password] %>",
  "port" => <%= config[:storage_add_fpt_port] %>
];

try {
  $storage = Uiza\Storage::add($params);

  print_r($storage);
} catch(\Uiza\Exception\ErrorResponse $e) {
  print($e);
}
?>
```
```java
import java.util.*;
import com.google.gson.*;

import io.uiza.Uiza;
import io.uiza.exception.*;
import io.uiza.model.Storage;
import io.uiza.model.Storage.*;

public class Main {

  public static void main(String[] args) {
    Uiza.appId = "<%= config[:app_id] %>";
    Uiza.authorization = "<%= config[:token] %>";

    Map<String, Object> params = new HashMap<>();
    params.put("name", "<%= config[:storage_add_fpt_name] %>");
    params.put("description", "<%= config[:storage_add_fpt_description] %>");
    params.put("storageType", StorageType.FTP.toString());
    params.put("host", "<%= config[:storage_add_fpt_host] %>");
    params.put("username", "<%= config[:storage_add_fpt_username] %>");
    params.put("password", "<%= config[:storage_add_fpt_password] %>");
    params.put("port", <%= config[:storage_add_fpt_port] %>);

    try {
      JsonObject response = Storage.add(params);
      System.out.println(response);
    } catch (UizaException e) {
      System.out.println("Status is: " + e.getStatusCode());
      System.out.println("Message is: " + e.getMessage());
      System.out.println("Description link is: " + e.getDescriptionLink());
    } catch (Exception e) {
      System.out.println(e);
    }
  }
}
```

```javascript
const uiza = require('uiza');

uiza.app_id('<%= config[:app_id] %>');
uiza.authorization('<%= config[:token] %>');

const params = {
  'name': '<%= config[:storage_add_fpt_name] %>',
  'description': '<%= config[:storage_add_fpt_description] %>',
  'storageType': '<%= config[:storage_add_fpt_storage_type] %>',
  'host': '<%= config[:storage_add_fpt_host] %>',
  'username': '<%= config[:storage_add_fpt_username] %>',
  'password': '<%= config[:storage_add_fpt_password] %>',
  'port': <%= config[:storage_add_fpt_port] %>
}

uiza.storage.add(params)
  .then((res) => {
    //Identifier of storage has been add
  }).catch((err) => {
    //Error
  });
```

```go
import (
  uiza "github.com/uizaio/api-wrapper-go"
  "github.com/uizaio/api-wrapper-go/storage"
)

func init() {
  Uiza.AppID = "<%= config[:app_id] %>"
  Uiza.Authorization = "<%= config[:token] %>"
}

params :=  &uiza.StorageAddParams{
  Name: uiza.String("<%= config[:storage_add_s3_name] %>"),
  Host: uiza.String("<%= config[:storage_add_s3_host] %>"),
  Port: <%= config[:storage_add_s3_port] %>,
  StorageType: "<%= config[:storage_add_s3_storage_type] %>",
  Username: uiza.String("<%= config[:storage_add_s3_username] %>"),
  Password: uiza.String("<%= config[:storage_add_s3_password] %>"),
  Description: uiza.String("<%= config[:storage_add_s3_description] %>"),
}

response, err := storage.Add(params)
if err != nil {
  log.Printf("%v\n", err)
} else {
  log.Printf("%v\n", response)
}
```

```csharp
using System;
using Uiza.Net.Configuration;
using Uiza.Net.Enums;
using Uiza.Net.Parameters;
using Uiza.Net.Services;

UizaConfiguration.SetupUiza(new UizaConfigOptions
{
  Authorization = "<%= config[:token] %>",
  AppId = "<%= config[:app_id] %>"
});

try
{
  var result = UizaServices.Storage.Add(new AddStorageParameter()
  {
    Name = "<%= config[:storage_add_s3_name] %>",
    Description = "<%= config[:storage_add_s3_description] %>",
    StorageType = "<%= config[:storage_add_s3_storage_type] %>",
    Host = "<%= config[:storage_add_s3_host] %>",
    Username = "<%= config[:storage_add_s3_username] %>",
    Password = "<%= config[:storage_add_s3_password] %>",
    Port = <%= config[:storage_add_s3_port] %>
  });

  Console.WriteLine(string.Format("Add New Storage Id = {0} Success", createResult.Data.id));
  Console.ReadLine();
}
catch (UizaException ex)
{
  Console.WriteLine(ex.Message);
  Console.ReadLine();
}
```

> **Add a S3 Storage**

```shell
curl -X POST \
  <%= config[:endpoint] %>/media/storage \
  -H 'Authorization: <%= config[:token] %>' \
  -H 'Content-Type: application/json' \
  -d '{
    "appId": "<%= config[:app_id] %>",
    "name": "<%= config[:storage_add_s3_name] %>",
    "description": "<%= config[:storage_add_s3_description] %>",
    "storageType": "<%= config[:storage_add_s3_storage_type] %>",
    "host": "<%= config[:storage_add_s3_host] %>",
    "username": "<%= config[:storage_add_s3_username] %>",
    "password": "<%= config[:storage_add_s3_password] %>",
    "port": <%= config[:storage_add_s3_port] %>
}'
```

```ruby
require "uiza"

Uiza.app_id = "<%= config[:app_id] %>"
Uiza.authorization = "<%= config[:token] %>"

params = {
  name: "<%= config[:storage_add_s3_name] %>",
  description: "<%= config[:storage_add_s3_description] %>",
  storageType: "<%= config[:storage_add_s3_storage_type] %>",
  host: "<%= config[:storage_add_s3_host] %>",
  username: "<%= config[:storage_add_s3_username] %>",
  password: "<%= config[:storage_add_s3_password] %>",
  port: <%= config[:storage_add_s3_port] %>
}

begin
  storage = Uiza::Storage.add params
  puts storage.id
  puts storage.name
rescue Uiza::Error::UizaError => e
  puts "description_link: #{e.description_link}"
  puts "code: #{e.code}"
  puts "message: #{e.message}"
rescue StandardError => e
  puts "message: #{e.message}"
end
```

```python
import uiza

from uiza.api_resources.storage import Storage
from uiza.exceptions import ServerException

uiza.app_id = "<%= config[:app_id] %>"
uiza.authorization = "<%= config[:token] %>"

storage_data = {
  "name": "<%= config[:storage_add_s3_name] %>",
  "description": "<%= config[:storage_add_s3_description] %>",
  "storageType": "<%= config[:storage_add_s3_storage_type] %>",
  "host": "<%= config[:storage_add_s3_host] %>",
  "username": "<%= config[:storage_add_s3_username] %>",
  "password": "<%= config[:storage_add_s3_password] %>",
  "port": <%= config[:storage_add_s3_port] %>
}

try:
  res, status_code = Storage().add(**storage_data)

  print("res: ", res)
except ServerException as e:
  raise e
except Exception as e:
  raise e
```

```php
<?php
require __DIR__."/../vendor/autoload.php";

Uiza\Base::setAppId("<%= config[:app_id] %>");
Uiza\Base::setAuthorization("<%= config[:token] %>");

$params = [
  "name" => "<%= config[:storage_add_s3_name] %>",
  "description" => "<%= config[:storage_add_s3_description] %>",
  "storageType" => "<%= config[:storage_add_s3_storage_type] %>",
  "host" => "<%= config[:storage_add_s3_host] %>",
  "username" => "<%= config[:storage_add_s3_username] %>",
  "password" => "<%= config[:storage_add_s3_password] %>",
  "port" => <%= config[:storage_add_s3_port] %>
];

try {
  $storage = Uiza\Storage::add($params);

  print_r($storage);
} catch(\Uiza\Exception\ErrorResponse $e) {
  print($e);
}
?>
```

```java
import java.util.*;
import com.google.gson.*;

import io.uiza.Uiza;
import io.uiza.exception.*;
import io.uiza.model.Storage;
import io.uiza.model.Storage.*;

public class Main {

  public static void main(String[] args) {
    Uiza.appId = "<%= config[:app_id] %>";
    Uiza.authorization = "<%= config[:token] %>";

    Map<String, Object> params = new HashMap<>();
    params.put("name", "<%= config[:storage_add_s3_name] %>");
    params.put("description", "<%= config[:storage_add_s3_description] %>");
    params.put("storageType", StorageType.S3.toString());
    params.put("host", "<%= config[:storage_add_s3_host] %>");
    params.put("username", "<%= config[:storage_add_s3_username] %>");
    params.put("password", "<%= config[:storage_add_s3_password] %>");
    params.put("port", <%= config[:storage_add_s3_port] %>);

    try {
      JsonObject response = Storage.add(params);
      System.out.println(response);
    } catch (UizaException e) {
      System.out.println("Status is: " + e.getStatusCode());
      System.out.println("Message is: " + e.getMessage());
      System.out.println("Description link is: " + e.getDescriptionLink());
    } catch (Exception e) {
      System.out.println(e);
    }
  }
}
```

```javascript
const uiza = require('uiza');

uiza.app_id('<%= config[:app_id] %>');
uiza.authorization('<%= config[:token] %>');

const params = {
  'name': '<%= config[:storage_add_s3_name] %>',
  'description': '<%= config[:storage_add_s3_description] %>',
  'storageType': '<%= config[:storage_add_s3_storage_type] %>',
  'host': '<%= config[:storage_add_s3_host] %>',
  'username': '<%= config[:storage_add_s3_username] %>',
  'password': '<%= config[:storage_add_s3_password] %>',
  'port': <%= config[:storage_add_s3_port] %>
}

uiza.storage.add(params)
  .then((res) => {
    //Identifier of storage has been add
  }).catch((err) => {
    //Error
  });
```

```go
import (
  uiza "github.com/uizaio/api-wrapper-go"
  "github.com/uizaio/api-wrapper-go/storage"
)

func init() {
  Uiza.AppID = "<%= config[:app_id] %>"
  Uiza.Authorization = "<%= config[:token] %>"
}

params :=  &uiza.StorageAddParams{
  Name: uiza.String("<%= config[:storage_add_fpt_name] %>"),
  Host: uiza.String("<%= config[:storage_add_fpt_host] %>"),
  Port: <%= config[:storage_add_fpt_port] %>,
  StorageType: "<%= config[:storage_add_fpt_storage_type] %>",
  Username: uiza.String("<%= config[:storage_add_fpt_username] %>"),
  Password: uiza.String("<%= config[:storage_add_fpt_password] %>"),
  Description: uiza.String("<%= config[:storage_add_fpt_description] %>"),
}

response, err := storage.Add(params)
if err != nil {
  log.Printf("%v\n", err)
} else {
  log.Printf("%v\n", response)
}
```

```csharp
using System;
using Uiza.Net.Configuration;
using Uiza.Net.Enums;
using Uiza.Net.Parameters;
using Uiza.Net.Services;

UizaConfiguration.SetupUiza(new UizaConfigOptions
{
  Authorization = "<%= config[:token] %>",
  AppId = "<%= config[:app_id] %>"
});

try
{
  var result = UizaServices.Storage.Add(new AddStorageParameter()
  {
    Name = "<%= config[:storage_add_fpt_name] %>",
    Description = "<%= config[:storage_add_fpt_description] %>",
    StorageType = "<%= config[:storage_add_fpt_storage_type] %>",
    Host = "<%= config[:storage_add_fpt_host] %>",
    Username = "<%= config[:storage_add_fpt_username] %>",
    Password = "<%= config[:storage_add_fpt_password] %>",
    Port = <%= config[:storage_add_fpt_port] %>
  });

  Console.WriteLine(string.Format("Add New Storage Id = {0} Success", createResult.Data.id));
  Console.ReadLine();
}
catch (UizaException ex)
{
  Console.WriteLine(ex.Message);
  Console.ReadLine();
}
```

> RESPONSE

```json
{
  "id": "<%= config[:storage_add_fpt_id] %>"
}
```

**HTTP Request**

<span class="post-button"> POST </span>
```<%= config[:endpoint] %>/media/storage```

**ARGUMENTS**

### FTP storage

| Parameter  | Description | Required |
|-------------|-------------|---------|
| **name** *string* | Storage's name. Used for references. | **Yes** |
| **host** *string* | Host name or IP Address of the FTP server. | **Yes** |
| **port** *int* | FTP Port. | **Yes** |
| **type** *string* | Must be set to `ftp` for this storage type. | **Yes** |
| **username** *string* | FTP username |  |
| **password** *string* | FTP password |  |
| **description** *string* | Storage's description |  |

### S3 storage

| Parameter   | Description | Required |
|-------------|-------------|---------|
| **name** *string* | Name of the storage| **Yes** |
| **host** *string* | Host name or IP Address of the S3 server. | **Yes** |
| **port** *int* | S3's port. Usually it's 443.  | **Yes** |
| **type** *string* | Must be set to `s3` for this storage type. | **Yes** |
| **awsAccessKey** *string* | AWS Access key ID. |  |
| **awsSecretKey** *string* | AWS Secret key ID. |  |
| **prefix** *string* | Prefix for objects store in this S3. |  |
| **bucket** *string* | Bucket data of S3. |  |
| **region** *string* | Storage's region, applicable for AWS S3. |  |
| **description** *string* | Storage's description |  |

**RETURNS**

Returns a storage identifier if the call succeeded.
